/* ------------------------------ Basic imports ----------------------------- */
import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  calendar: {
    width: '90%',
    alignSelf: 'center',
    marginBottom: 70,
    marginTop: 20,
  },
});

export default styles;
